name: Create New Ritual Log

on:
  workflow_dispatch:

jobs:
  new-log:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Get latest day number
        id: get_day
        run: |
          latest=$(ls | grep '^day-[0-9]\+\.md$' | sed 's/day-\([0-9]\+\).md/\1/' | sort -n | tail -1)
          if [ -z "$latest" ]; then
            next=1
          else
            next=$((latest + 1))
          fi
          echo "day_num=$next" >> $GITHUB_ENV

      - name: Create new ritual file
        run: |
          echo "# Ritual Log – Day ${{ env.day_num }}" > day-${{ env.day_num }}.md
          echo "" >> day-${{ env.day_num }}.md
          echo "**Date:** $(date +%Y-%m-%d)" >> day-${{ env.day_num }}.md
          echo "**Start Time:**" >> day-${{ env.day_num }}.md
          echo "**Focus of the Session:**" >> day-${{ env.day_num }}.md
          echo "**What I Did:**" >> day-${{ env.day_num }}.md
          echo "**Mood (Before → After):**" >> day-${{ env.day_num }}.md
          echo "**Distractions / Resistance:**" >> day-${{ env.day_num }}.md
          echo "**One Line Summary:**" >> day-${{ env.day_num }}.md

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add day-${{ env.day_num }}.md
          git commit -m "Add ritual log for day ${{ env.day_num }}"
          git push
